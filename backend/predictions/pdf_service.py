"""
predictions/pdf_service.py

PDF generation service for saved markets with branding and watermarks.
PREMIUM VERSION - Enhanced styling and professional features
"""
from io import BytesIO
from datetime import datetime
from reportlab.lib.pagesizes import A4
from reportlab.lib import colors
from reportlab.lib.units import cm
from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
from reportlab.lib.enums import TA_CENTER, TA_LEFT
from reportlab.platypus import SimpleDocTemplate, Table, TableStyle, Paragraph, Spacer, Image, PageBreak
from reportlab.pdfbase import pdfmetrics
from reportlab.pdfbase.ttfonts import TTFont
from reportlab.pdfgen import canvas
import os
import uuid


def create_section_divider(title=""):
    """Create professional section divider with optional title"""
    elements = []
    elements.append(Spacer(1, 0.4*cm))
    
    # Divider line with gold color
    divider_table = Table([['']], colWidths=[18*cm], rowHeights=[0.05*cm])
    divider_table.setStyle(TableStyle([
        ('BACKGROUND', (0, 0), (-1, -1), colors.HexColor('#D4AF37')),
        ('GRID', (0, 0), (-1, -1), 0, colors.white),  # No borders
    ]))
    elements.append(divider_table)
    
    if title:
        elements.append(Spacer(1, 0.3*cm))
        # Section title
        section_title = Paragraph(
            f"<b>{title}</b>",
            ParagraphStyle(
                'SectionTitle',
                fontName='Helvetica-Bold',
                fontSize=12,
                leading=16,
                textColor=colors.HexColor('#D4AF37'),
                spaceAfter=15,
                spaceBefore=10,
            )
        )
        elements.append(section_title)
    
    elements.append(Spacer(1, 0.4*cm))
    return elements


def create_premium_table_style():
    """Create professional premium table styling"""
    return TableStyle([
        # Header styling
        ('BACKGROUND', (0, 0), (-1, 0), colors.HexColor('#D4AF37')),
        ('TEXTCOLOR', (0, 0), (-1, 0), colors.white),
        ('ALIGN', (0, 0), (-1, 0), 'CENTER'),
        ('VALIGN', (0, 0), (-1, 0), 'MIDDLE'),
        ('FONTNAME', (0, 0), (-1, 0), 'Helvetica-Bold'),
        ('FONTSIZE', (0, 0), (-1, 0), 12),
        ('BOTTOMPADDING', (0, 0), (-1, 0), 16),
        ('TOPPADDING', (0, 0), (-1, 0), 16),
        
        # Body rows styling
        ('FONTNAME', (0, 1), (-1, -1), 'Helvetica'),
        ('FONTSIZE', (0, 1), (-1, -1), 11),
        ('VALIGN', (0, 1), (-1, -1), 'MIDDLE'),
        ('ALIGN', (0, 1), (-1, -1), 'LEFT'),
        
        # Padding for readability
        ('LEFTPADDING', (0, 1), (-1, -1), 12),
        ('RIGHTPADDING', (0, 1), (-1, -1), 12),
        ('TOPPADDING', (0, 1), (-1, -1), 12),
        ('BOTTOMPADDING', (0, 1), (-1, -1), 12),
        
        # Alternating row colors (subtle)
        ('ROWBACKGROUNDS', (0, 1), (-1, -1), 
            [colors.white, colors.HexColor('#FAFAFA')]),
        
        # Subtle grid lines (not heavy borders)
        ('GRID', (0, 0), (-1, -1), 0.5, colors.HexColor('#E5E5E5')),
        
        # Confidence column special styling
        ('ALIGN', (2, 1), (2, -1), 'CENTER'),  # Center align confidence
        ('FONTNAME', (2, 1), (2, -1), 'Helvetica-Bold'),  # Bold confidence values
        ('TEXTCOLOR', (2, 1), (2, -1), colors.HexColor('#D4AF37')),  # Gold confidence text
    ])


def create_professional_footer():
    """Create premium footer section"""
    elements = []
    
    # Add space before footer
    elements.append(Spacer(1, 0.8*cm))
    
    # Footer divider line
    footer_divider = Table([['']], colWidths=[17*cm])
    footer_divider.setStyle(TableStyle([
        ('LINEABOVE', (0, 0), (-1, -1), 1, colors.HexColor('#D4AF37')),
    ]))
    elements.append(footer_divider)
    
    elements.append(Spacer(1, 0.4*cm))
    
    # Footer text with professional styling
    footer_style = ParagraphStyle(
        'Footer',
        fontName='Helvetica',
        fontSize=9,
        leading=14,
        textColor=colors.HexColor('#666666'),
        alignment=TA_CENTER,
    )
    
    elements.append(Paragraph(
        "© 2026 Bashiri - AI Football Predictions. All rights reserved.",
        footer_style
    ))
    elements.append(Paragraph(
        "This document is generated by Bashiri AI. Predictions are for entertainment purposes only.",
        footer_style
    ))
    
    return elements


def generate_saved_markets_pdf(saved_markets, tab_name="All Markets"):
    """
    Generate a PDF document with saved markets data.
    
    Args:
        saved_markets: List of saved market dictionaries with match and AI data
        tab_name: Name of the tab/category being generated
    
    Returns:
        BytesIO: PDF file content
    """
    buffer = BytesIO()
    
    # Generate unique document ID
    document_id = str(uuid.uuid4())[:8].upper()
    
    # Custom page function to add watermark and page numbers
    def add_page_footer(canvas, doc):
        # Add page number
        page_num = canvas.getPageNumber()
        canvas.setFont('Helvetica', 9)
        canvas.setFillColor(colors.HexColor('#999999'))
        page_text = f"Page {page_num}"
        text_width = canvas.stringWidth(page_text, 'Helvetica', 9)
        
        # Draw centered at bottom
        canvas.drawString(
            (A4[0] - text_width) / 2,
            0.75 * cm,
            page_text
        )
        
        # Add premium watermark logo with reduced opacity
        try:
            # Try to find the logo in the frontend public directory
            logo_path = os.path.join(os.path.dirname(__file__), '..', '..', 'frontend', 'public', 'icon-192.png')
            if os.path.exists(logo_path):
                canvas.saveState()
                # Set reduced transparency for premium subtle watermark
                canvas.setFillAlpha(0.08)  # 8% opacity - very subtle and premium
                # Center the logo on the page
                page_width, page_height = A4
                logo = Image(logo_path, width=10*cm, height=10*cm)  # Slightly larger for better visibility
                logo.drawOn(canvas, (page_width - 10*cm) / 2, (page_height - 10*cm) / 2)
                canvas.restoreState()
        except Exception:
            # If logo not found, skip watermark
            pass
    
    doc = SimpleDocTemplate(
        buffer,
        pagesize=A4,
        rightMargin=2*cm,
        leftMargin=2*cm,
        topMargin=2*cm,
        bottomMargin=1.5*cm,  # Reduced for page numbers
        onFirstPage=add_page_footer,
        onLaterPages=add_page_footer
    )
    
    # Custom styles - PREMIUM TYPOGRAPHY
    styles = getSampleStyleSheet()
    title_style = ParagraphStyle(
        'CustomTitle',
        parent=styles['Heading1'],
        fontName='Helvetica-Bold',  # Using available bold font
        fontSize=28,  # Increased from 24 for more impact
        textColor=colors.HexColor('#D4AF37'),
        spaceAfter=36,  # Increased spacing
        alignment=TA_CENTER,
        leading=40  # Better line height (1.4x)
    )
    
    subtitle_style = ParagraphStyle(
        'CustomSubtitle',
        parent=styles['Heading2'],
        fontName='Helvetica-Bold',
        fontSize=18,  # Increased from 16
        textColor=colors.HexColor('#CFAF7B'),
        spaceAfter=24,  # Increased spacing
        alignment=TA_CENTER,
        leading=26  # Better line height (1.4x)
    )
    
    normal_style = ParagraphStyle(
        'CustomNormal',
        parent=styles['Normal'],
        fontName='Helvetica',
        fontSize=12,
        textColor=colors.HexColor('#333333'),
        spaceAfter=14,  # Increased spacing
        leading=18  # 1.5 line spacing
    )
    
    header_style = ParagraphStyle(
        'CustomHeader',
        parent=styles['Heading3'],
        fontName='Helvetica-Bold',
        fontSize=16,  # Increased from 14
        textColor=colors.HexColor('#D4AF37'),
        spaceAfter=12,  # Increased spacing
        spaceBefore=24,  # Increased spacing before
        leading=22  # Better line height (1.4x)
    )
    
    market_style = ParagraphStyle(
        'CustomMarket',
        parent=styles['Normal'],
        fontName='Helvetica',
        fontSize=12,
        textColor=colors.HexColor('#666666'),
        spaceAfter=10,  # Increased spacing
        leading=18  # 1.5 line spacing
    )
    
    ai_style = ParagraphStyle(
        'CustomAI',
        parent=styles['Normal'],
        fontName='Helvetica-Bold',  # Bold for AI predictions
        fontSize=12,
        textColor=colors.HexColor('#00FF87'),
        spaceAfter=10,  # Increased spacing
        leading=18  # 1.5 line spacing
    )
    
    # Premium footer style
    footer_style = ParagraphStyle(
        'Footer',
        fontName='Helvetica',
        fontSize=9,  # Increased from 8 for better readability
        textColor=colors.HexColor('#999999'),
        alignment=TA_CENTER,
        leading=12  # Better line height
    )
    
    # Build content
    content = []
    
    # Professional header with gold background
    header_data = [[Paragraph("Bashiri - AI Football Predictions", title_style)]]
    header_table = Table(header_data, colWidths=[17*cm])
    header_table.setStyle(TableStyle([
        ('BACKGROUND', (0, 0), (-1, -1), colors.HexColor('#D4AF37')),
        ('ALIGN', (0, 0), (-1, -1), 'CENTER'),
        ('VALIGN', (0, 0), (-1, -1), 'MIDDLE'),
        ('TOPPADDING', (0, 0), (-1, -1), 25),
        ('BOTTOMPADDING', (0, 0), (-1, -1), 25),
    ]))
    content.append(header_table)
    content.append(Spacer(1, 0.4*cm))
    
    # Subtitle with document metadata
    metadata_text = f"""
    <b>Saved Markets - {tab_name}</b><br/>
    Generated: {datetime.now().strftime('%B %d, %Y at %I:%M %p')}<br/>
    Document ID: {document_id}
    """
    content.append(Paragraph(metadata_text, subtitle_style))
    content.append(Spacer(1, 0.6*cm))
    
    # Group by match
    from collections import defaultdict
    grouped = defaultdict(list)
    for market in saved_markets:
        match_id = market['match']['id']
        grouped[match_id].append(market)
    
    # Generate content for each match
    match_count = len(grouped)
    for idx, (match_id, markets) in enumerate(grouped.items()):
        match = markets[0]['match']
        
        # Add section divider for subsequent matches
        if idx > 0:
            content.extend(create_section_divider())
        
        # Match header
        content.append(Paragraph(
            f"{match['home_team']['name']} vs {match['away_team']['name']}",
            header_style
        ))
        content.append(Paragraph(
            f"{match['league']['name']} • {datetime.fromisoformat(match['kickoff_at']).strftime('%B %d, %Y %I:%M %p')}",
            normal_style
        ))
        
        # Markets table
        market_data = [['Market Type', 'AI Prediction', 'Confidence']]
        for market in markets:
            market_key = market['market_key']
            ai_pick = market.get('ai_pick', 'N/A')
            confidence = market.get('ai_confidence')
            conf_str = f"{confidence}%" if confidence else 'N/A'
            
            # Convert market key to readable label
            market_labels = {
                "1X2": "Matokeo ya Mechi",
                "DOUBLE_CHANCE": "Double Chance",
                "DRAW_NO_BET": "Draw No Bet",
                "OVER_UNDER_0_5": "Over/Under 0.5",
                "OVER_UNDER_1_5": "Over/Under 1.5",
                "OVER_UNDER_2_5": "Over/Under 2.5",
                "OVER_UNDER_3_5": "Over/Under 3.5",
                "OVER_UNDER_4_5": "Over/Under 4.5",
                "BTTS": "Timu Zote Kufunga (BTTS)",
            }
            market_label = market_labels.get(market_key, market_key)
            
            market_data.append([market_label, ai_pick, conf_str])
        
        # Create table with premium styling
        table = Table(market_data, colWidths=[5.5*cm, 4*cm, 2.5*cm])  # Better column distribution
        table.setStyle(create_premium_table_style())
        
        content.append(table)
        content.append(Spacer(1, 0.8*cm))  # Increased spacing between tables
    
    # Professional footer with branding
    content.extend(create_professional_footer())
    
    # Build PDF
    doc.build(content)
    buffer.seek(0)
    return buffer
